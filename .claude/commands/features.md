---
name: features
description: Show shipped and planned features catalog, or detail a specific feature by ID
---

Show the feature catalog for the fin-portfolio project.

Read docs/features/README.md and present:

1. **Shipped features** — table with ID, name, ship date
2. **Planned features** — table with ID, name, priority

If $ARGUMENTS is a feature ID (e.g. "F-02"), read the corresponding feature doc and show its full details instead.
